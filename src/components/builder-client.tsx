'use client';

import Link from 'next/link';
import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import SocialLinksEditor from '@/components/social-links-editor';
import BusinessBrandAssets from '@/components/business-brand-assets';
import {defaultOpenStatusBlocks,normalizeOpenStatusPageConfig,type OpenStatusBlock,type OpenStatusPageConfig,type OpenStatusSocial} from '@/lib/openstatus-page-config';

type Business={id:string;name:string;tagline:string|null;slug:string|null;avatar_url:string|null;header_url:string|null};type BlockTemplate={id:string;title:string;sub:string;icon:string;tone:string};
const BLOCK_LIBRARY:BlockTemplate[]=[{id:'call',title:'Call',sub:'Tap to call us',icon:'',tone:'light'},{id:'gift-cards',title:'Gift Cards',sub:'Buy a gift card',icon:'',tone:'glass'},{id:'catering',title:'Catering',sub:'Catering & large orders',icon:'',tone:'light'},{id:'shop',title:'Shop',sub:'Shop online',icon:'',tone:'glass'},{id:'events',title:'Events',sub:'See upcoming events',icon:'',tone:'light'},{id:'careers',title:'Careers',sub:'Join our team',icon:'',tone:'glass'},{id:'email',title:'Email',sub:'Send us a message',icon:'',tone:'light'},{id:'custom',title:'Custom Link',sub:'Anything your customers need',icon:'',tone:'light'}];
const placeholders:Record<string,string>={order:'squareup.com/store/your-business',menu:'yourbusiness.com/menu',map:'123 Main St, Franklin, TN',book:'calendly.com/your-business',website:'yourbusiness.com',call:'tel:+16155551234',email:'mailto:hello@yourbusiness.com'};const socialMark=(label:string)=>({Instagram:'IG',Facebook:'f',TikTok:'TT',YouTube:'YT',LinkedIn:'in',Pinterest:'P',X:'X',Threads:'@'} as Record<string,string>)[label]||label.slice(0,2);function Mark(){return <svg viewBox="0 0 100 100" width="27" height="27"><circle cx="50" cy="50" r="48"/><circle cx="50" cy="50" r="21" fill="#F7F7F3"/><circle cx="50" cy="44" r="7.4"/><path d="M45.2 50.2h9.6l2.2 16.3H43z"/></svg>}
export default function BuilderClient(){const router=useRouter();const[business,setBusiness]=useState<Business|null>(null);const[blocks,setBlocks]=useState<OpenStatusBlock[]>(defaultOpenStatusBlocks);const[bg,setBg]=useState('warm');const[socials,setSocials]=useState<OpenStatusSocial[]>([]);const[location,setLocation]=useState('');const[tags,setTags]=useState<string[]>([]);const[tagDraft,setTagDraft]=useState('');const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[saved,setSaved]=useState(false);const[error,setError]=useState('');const[showLibrary,setShowLibrary]=useState(false);const[draggedId,setDraggedId]=useState<string|null>(null);
useEffect(()=>{void(async()=>{const{data:u}=await supabase.auth.getUser();if(!u.user){router.replace('/login');return}const c=normalizeOpenStatusPageConfig(u.user.user_metadata?.openstatus_page);setBlocks(c.blocks);setBg(c.bg);setSocials(c.socials);setLocation(c.location??'');setTags(c.tags??[]);const{data:b}=await supabase.from('businesses').select('id,name,tagline,slug,avatar_url,header_url').eq('user_id',u.user.id).maybeSingle();if(!b){router.replace('/setup');return}setBusiness(b);setLoading(false)})()},[router]);const dirty=()=>setSaved(false);const reorder=(a:string,z:string)=>{if(a===z)return;setBlocks(v=>{const x=v.findIndex(b=>b.id===a),y=v.findIndex(b=>b.id===z);if(x<0||y<0)return v;const n=[...v];const[m]=n.splice(x,1);n.splice(y,0,m);return n});dirty()};const toggle=(id:string)=>{setBlocks(v=>v.map(b=>b.id===id?{...b,on:!b.on}:b));dirty()};const edit=(id:string,k:'title'|'sub'|'url',value:string)=>{setBlocks(v=>v.map(b=>b.id===id?{...b,[k]:value}:b));dirty()};const remove=(id:string)=>{setBlocks(v=>v.filter(b=>b.id!==id));dirty()};const addBlock=(t:BlockTemplate)=>{let id=t.id,n=2;while(blocks.some(b=>b.id===id))id=`${t.id}-${n++}`;setBlocks(v=>[...v,{...t,id,on:true,url:''}]);setShowLibrary(false);dirty()};const addTag=()=>{const t=tagDraft.trim().replace(/^#/,'');if(!t||tags.length>=5||tags.some(x=>x.toLowerCase()===t.toLowerCase()))return;setTags(v=>[...v,t]);setTagDraft('');dirty()};const save=async()=>{setSaving(true);setError('');const config:OpenStatusPageConfig={blocks,bg,socials,location:location.trim(),tags};const{error:e}=await supabase.auth.updateUser({data:{openstatus_page:config}});setSaving(false);if(e){setError(e.message);return}setSaved(true)};const asset=(k:'avatar'|'header',v:string|null|undefined)=>v?.startsWith('storage:')?`/api/assets?businessId=${business?.id}&kind=${k}&v=${encodeURIComponent(v)}`:v||'';const logo=asset('avatar',business?.avatar_url),cover=asset('header',business?.header_url),initials=business?.name?.split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase()||'OS';if(loading)return <main className="grid min-h-screen place-items-center bg-[#F5F3ED]">Loading your page builder…</main>;
return <main className="min-h-screen bg-[#F5F3ED] text-[#101010]" style={{fontFamily:'var(--font-poppins)'}}><header className="sticky top-0 z-30 border-b border-black/8 bg-[#F5F3ED]/90 backdrop-blur-2xl"><div className="mx-auto flex w-[min(96%,1440px)] flex-wrap items-center gap-3 py-3 sm:flex-nowrap sm:justify-between sm:py-4"><Link href="/" className="flex shrink-0 items-center gap-2 font-bold"><Mark/><span>OpenStatus</span></Link><div className="ml-auto flex min-w-0 items-center justify-end gap-1.5 sm:gap-2">{business?.slug?<Link href={`/${business.slug}`} target="_blank" className="whitespace-nowrap rounded-full border border-black/10 bg-white/70 px-3 py-2 text-[11px] font-semibold sm:px-4 sm:py-2.5 sm:text-xs">View live</Link>:null}<Link href="/dashboard" className="hidden whitespace-nowrap rounded-full px-3 py-2 text-[11px] font-semibold sm:inline-flex sm:px-4 sm:py-2.5 sm:text-xs">Dashboard</Link><button onClick={save} disabled={saving} className="whitespace-nowrap rounded-full bg-black px-3.5 py-2.5 text-[11px] font-bold text-white disabled:opacity-40 sm:px-5 sm:py-3 sm:text-xs">{saving?'Publishing…':saved?'Published':'Publish'}</button></div></div></header><div className="mx-auto grid w-[min(96%,1440px)] gap-5 py-5 lg:grid-cols-[220px_minmax(420px,1fr)_420px]"><aside className="rounded-[28px] bg-white/65 p-4"><span className="text-[9px] font-bold tracking-[.14em] text-black/35">PAGE BUILDER</span><nav className="mt-4 space-y-1"><div className="rounded-[16px] bg-black px-3 py-3 text-sm font-semibold text-white">Edit profile</div><Link href="/dashboard" className="block rounded-[16px] px-3 py-3 text-sm font-semibold">Status</Link><Link href="/analytics" className="block rounded-[16px] px-3 py-3 text-sm font-semibold">Analytics</Link><Link href="/settings" className="block rounded-[16px] px-3 py-3 text-sm font-semibold">Settings</Link></nav></aside><section className="rounded-[30px] border border-black/8 bg-white/55 p-5 md:p-7"><div className="flex items-end justify-between gap-4"><div><span className="text-[9px] font-bold tracking-[.14em] text-black/35">EDIT PROFILE</span><h1 className="mt-2 text-4xl font-semibold tracking-[-.055em]">Make it yours.</h1></div><button onClick={()=>setShowLibrary(true)} className="rounded-full bg-black px-5 py-3 text-xs font-bold text-white">+ Add link</button></div>{business?<div className="mt-7"><BusinessBrandAssets business={business} onChange={next=>{setBusiness(c=>c?{...c,avatar_url:next.avatar_url,header_url:next.header_url}:c);dirty()}}/></div>:null}<div className="mt-4 rounded-[24px] bg-[#F5F3ED] p-4"><strong className="text-sm">Profile details</strong><label className="mt-3 block text-[9px] font-bold uppercase tracking-[.1em] text-black/35">Location<input value={location} onChange={e=>{setLocation(e.target.value);dirty()}} placeholder="Franklin, Tennessee" className="mt-1 w-full rounded-[14px] border border-black/8 bg-white px-3 py-3 text-xs font-normal normal-case tracking-normal"/></label><div className="mt-3"><span className="text-[9px] font-bold uppercase tracking-[.1em] text-black/35">Tags · up to 5</span><div className="mt-2 flex gap-2"><input value={tagDraft} onChange={e=>setTagDraft(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();addTag()}}} placeholder="Coffee, Breakfast, Dog Friendly…" className="min-w-0 flex-1 rounded-[14px] border border-black/8 bg-white px-3 py-3 text-xs"/><button onClick={addTag} className="rounded-[14px] bg-black px-4 text-xs font-bold text-white">Add</button></div><div className="mt-2 flex flex-wrap gap-1.5">{tags.map(t=><button key={t} onClick={()=>{setTags(v=>v.filter(x=>x!==t));dirty()}} className="rounded-full bg-white px-3 py-1.5 text-[10px] font-semibold">{t} ×</button>)}</div></div></div><div className="mt-6 flex items-center justify-between"><div><strong className="text-sm">Links & actions</strong><p className="text-xs text-black/40">Choose an action, then paste its destination.</p></div><button onClick={()=>setShowLibrary(true)} className="rounded-full border border-black/10 bg-white px-4 py-2 text-xs font-bold">+ Add link</button></div><div className="mt-3 space-y-3">{blocks.map(b=><div key={b.id} data-block-id={b.id} onDragOver={e=>e.preventDefault()} onDragEnter={()=>{if(draggedId)reorder(draggedId,b.id)}} className={`rounded-[22px] border border-black/8 bg-white p-4 ${!b.on?'opacity-50':''}`}><div className="flex items-center gap-3"><button draggable onDragStart={()=>setDraggedId(b.id)} onDragEnd={()=>setDraggedId(null)} className="cursor-grab text-xl text-black/25">⋮⋮</button><div className="grid h-11 w-11 place-items-center rounded-[14px] bg-[#F2EFE8] text-[10px] font-bold uppercase text-black/45">{b.title.slice(0,2)}</div><strong className="flex-1 text-sm">{b.title}</strong><button onClick={()=>toggle(b.id)} className={`rounded-full px-3 py-2 text-[9px] font-bold ${b.on?'bg-[#C8FF62]/65':'bg-black/5'}`}>{b.on?'ON':'OFF'}</button><button onClick={()=>remove(b.id)} className="h-8 w-8 rounded-full bg-black/5">×</button></div><div className="mt-3 grid gap-2 sm:grid-cols-2"><input value={b.title} onChange={e=>edit(b.id,'title',e.target.value)} className="rounded-[14px] bg-[#F5F3ED] px-3 py-2.5 text-xs"/><input value={b.sub} onChange={e=>edit(b.id,'sub',e.target.value)} className="rounded-[14px] bg-[#F5F3ED] px-3 py-2.5 text-xs"/></div><div className="mt-2 flex gap-2"><input value={b.url||''} onChange={e=>edit(b.id,'url',e.target.value)} placeholder={placeholders[b.id]||'https://...'} className="min-w-0 flex-1 rounded-[14px] border border-black/8 bg-white px-3 py-3 text-xs"/><span className={`grid min-w-16 place-items-center rounded-[14px] px-3 text-[9px] font-bold ${b.url?.trim()?'bg-[#C8FF62]/55':'bg-black/5 text-black/35'}`}>{b.url?.trim()?'ADDED':'ADD URL'}</span></div></div>)}</div><SocialLinksEditor socials={socials} onChange={n=>{setSocials(n);dirty()}}/>{error?<p className="mt-4 rounded-[14px] bg-[#F8AE9D]/55 p-3 text-xs">{error}</p>:null}</section><aside className="rounded-[30px] bg-[#E8E4DA] p-5"><div className="mb-3 flex justify-between text-[9px] font-bold tracking-[.13em] text-black/35"><span>LIVE PREVIEW</span><span>Mobile</span></div><div className="relative mx-auto min-h-[700px] max-w-[360px] overflow-hidden rounded-[42px] border-[8px] border-black bg-[#333] shadow-[0_28px_70px_rgba(0,0,0,.22)]">{cover?<img src={cover} alt="" className="absolute inset-0 h-full w-full object-cover"/>:<div className={`absolute inset-0 ${bg==='warm'?'bg-[#E8D5BF]':bg==='blue'?'bg-[#CBD9FF]':bg==='lime'?'bg-[#DFFFA8]':'bg-[#333]'}`}/>}<div className="absolute inset-0 bg-gradient-to-b from-black/45 via-black/25 to-black/45"/><div className="relative px-3 pb-5 pt-14 text-white"><div className="text-center">{logo?<img src={logo} alt="" className="mx-auto h-20 w-20 rounded-full border-4 border-white bg-white object-cover shadow-xl"/>:<div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-white font-bold text-black">{initials}</div>}<strong className="mt-3 block text-2xl">{business?.name}</strong>{location?<span className="mt-1 block text-[11px] text-white/85">{location}</span>:null}<div className="mt-2 flex flex-wrap justify-center gap-1">{tags.map(t=><span key={t} className="rounded-full border border-white/25 bg-black/20 px-2 py-1 text-[8px]">{t}</span>)}</div></div><div className="mt-5 rounded-[20px] border border-white/50 bg-white/70 p-3 text-black backdrop-blur-xl"><span className="text-[8px] font-bold text-[#2E7D5B]">LIVE STATUS</span><strong className="mt-1 block text-xl">Open now</strong><span className="text-[10px] text-black/45">Closes at 4:00 PM</span></div><div className="mt-2 grid grid-cols-2 gap-2">{blocks.filter(b=>b.on).map(b=><div key={b.id} className={`${b.id==='website'||b.id==='map'?'col-span-2':''} rounded-[18px] border border-white/50 bg-white/65 p-3 text-black backdrop-blur-xl`}><strong className="block text-xs">{b.title}</strong><span className="text-[9px] opacity-45">{b.sub}</span></div>)}</div>{socials.some(s=>s.on&&s.url)&&<div className="mt-3 flex justify-center gap-2">{socials.filter(s=>s.on&&s.url).map(s=><span key={s.id} className="grid h-8 w-8 place-items-center rounded-full bg-white/70 text-[9px] font-bold text-black">{socialMark(s.label)}</span>)}</div>}<div className="mt-4 text-center text-[8px] tracking-[.13em] text-white/50">POWERED BY OPENSTATUS</div></div></div></aside></div>{showLibrary&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4 backdrop-blur-sm" onMouseDown={()=>setShowLibrary(false)}><div className="w-full max-w-xl rounded-[30px] bg-[#F8F7F2] p-6 shadow-2xl" onMouseDown={e=>e.stopPropagation()}><div className="flex justify-between"><div><span className="text-[9px] font-bold tracking-[.14em] text-black/35">ADD A LINK</span><h2 className="mt-2 text-3xl font-semibold">What should customers do?</h2></div><button onClick={()=>setShowLibrary(false)} className="h-9 w-9 rounded-full bg-black/5">×</button></div><div className="mt-5 grid gap-2 sm:grid-cols-2">{BLOCK_LIBRARY.map(t=><button key={t.id} onClick={()=>addBlock(t)} className="rounded-[18px] border border-black/8 bg-white p-4 text-left hover:shadow-md"><strong className="block text-sm">{t.title}</strong><span className="mt-1 block text-[10px] text-black/40">{t.sub}</span></button>)}</div></div></div>}</main>